[project]
name = "astrbot-plugin-phone-mcp"
version = "0.1.0"
description = "AstrBot plugin dependencies for phone MCP bridge"
requires-python = ">=3.10"
dependencies = [
    "websockets>=16.0",
]

[tool.uv]
package = false
